import { EventEmitterType } from 'belter';
export declare const CLASS: {
    VISIBLE: string;
    INVISIBLE: string;
};
export declare const containerTemplate: ({ uid, frame, prerenderFrame, doc, event, }: {
    uid: string;
    frame: HTMLElement;
    prerenderFrame: HTMLElement;
    doc: Document;
    event: EventEmitterType;
}) => HTMLElement;
