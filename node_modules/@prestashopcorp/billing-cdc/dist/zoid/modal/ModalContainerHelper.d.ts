import { EventEmitterType } from 'belter';
export declare const CLASS: {
    VISIBLE: string;
    INVISIBLE: string;
};
export declare const containerTemplate: ({ uid, props, frame, prerenderFrame, doc, event, }: {
    uid: string;
    props: unknown & {
        cspNonce: string;
        type: string;
    };
    frame: HTMLElement;
    prerenderFrame: HTMLElement;
    doc: Document;
    event: EventEmitterType;
}) => HTMLElement;
